import styled from 'styled-components';

export const Container = styled.div`
    flex: 1;
    margin: 20px;
    gap: 10px;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    align-items: center;
`;
